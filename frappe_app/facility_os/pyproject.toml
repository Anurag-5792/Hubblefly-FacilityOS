[project]
name = "facility_os"
version = "0.1.0"
description = "Hubblefly FacilityOS operational and MIS layer for ERPNext"
requires-python = ">=3.10"
authors = [{ name = "Hubblefly Technologies Limited" }]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=15.0.0"
erpnext = ">=15.0.0"
