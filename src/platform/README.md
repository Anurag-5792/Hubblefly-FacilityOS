# Platform adapters

Future authorised work packages will place provider/infrastructure adapters here, including
authentication, database, security, object storage, background jobs, observability and request
context.

The target domain must not directly depend on Supabase, Vercel or ERPNext SDK/HTTP details.
W0-01 adds only the environment-contract foundation under `config/`.
